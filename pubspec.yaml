name: app
description: "(:"
publish_to: 'none'
version: 1.0.0+1
environment:
  sdk: ^3.6.0-254.0.dev
dependencies:
  flutter:
    sdk: flutter
  table_calendar: ^3.2.0
  cupertino_icons: ^1.0.8
  flutter_svg: ^2.0.17
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
flutter:
  uses-material-design: true
  assets:
    - lib/icon/
  # UI dagi fontni topaolmadim!